// Add custom Cypress commands for your project here.
// This file intentionally left minimal so tests in `cypress/e2e` can import it safely.

// Example placeholder (uncomment and customize if needed):
// Cypress.Commands.add('login', (username, password) => {
//   cy.request('POST', '/api/login', { username, password })
// })

// No-op file by default.
